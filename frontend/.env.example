# BluSanta Frontend Environment Configuration
# This file should be placed in the frontend directory

# Backend API URL (update with your VM's static IP for production)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# For production, use:
# NEXT_PUBLIC_BACKEND_URL=http://<YOUR_STATIC_IP>:3001
